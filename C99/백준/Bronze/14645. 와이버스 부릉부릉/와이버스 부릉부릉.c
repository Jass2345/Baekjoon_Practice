#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
    int N = 0, K = 0, A = 0, B = 0;
    scanf("%d %d", &N, &K);
    for (int i = 0; i < N; i++) {
        scanf("%d %d", &A, &B);
    }
    printf("비와이");

    return 0;
}